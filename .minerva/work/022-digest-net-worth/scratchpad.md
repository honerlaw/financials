# Scratchpad: 022-digest-net-worth

## Balanced decisions 2026-08-24

- [escalated to user] intake requirements: (a) excluded accounts stay listed vs hidden, (b) unvested equity in net worth, (c) exclusion mechanism — user chose still-listed + suffix, vested-only, Doppler config list. Escalation counter: 1.
- [reviewed — folded] scope check: one unit, not decomposed (Skeptic `revise`). Folded: A2P campaign-description prose added to scope alongside the sample messages (unit 020 precedent updated both); `(unvested excluded)` suffix added so a line that does not reconcile with the total says why; net-worth line placement pinned (after Balances, before opt-out). Skeptic's headline point — liabilities must be subtracted — was already in the design but absent from that gate's artifact, so not a real gap. Skeptic confirmed no forgotten call site and that the file set matches unit 020's single-unit precedent.
- [reviewed — folded] approach: A (pure derivation over one widened query) over B (SQL CASE aggregate) and C (shared `app/networth.py`) — Skeptic `revise`. Folded the load-bearing correctness catch: `vested_value` is the sum of only holdings reporting a vested figure, so substituting it drops plain brokerage value in a mixed account; switched to `current_balance - unvested_value`. Also folded: exclusion match key pinned to `institution:account` with both halves required; unmatched-pattern logging moved to the impure shell so the derivations stay pure; credit/loan sign gets fixture-backed tests as the first semantic read of `Account.type` in the codebase.
- [decided] whole-proposal soundness: internally consistent; no public interface or cross-cutting contract introduced — `digest_body`/`account_line` are module-internal with only in-repo callers (both Skeptics grepped the call sites independently). Solo gate.

## Implementation notes 2026-08-24

- `_account_balances` became `_account_rows`, returning an `AccountRow` NamedTuple. The display tuples stayed positional so `account_line(*a)` survives and existing 4-tuple test fixtures keep working; the type/slug/unvested fields the total needs ride on the row, not on the display tuple.
- Introduced `_digest_parts(session, config, today)` as the single impure seam both send paths call, so the scheduled digest and the button cannot drift in what the message contains. `_warn_unmatched` logs; the derivations stay pure.
- **Deviation from the proposal, deliberate:** `account_net_worth` clamps an asset's contribution at zero. `current_balance` and `unvested_value` come from different Plaid endpoints (`accounts/balance/get` vs `investments/holdings/get`), so a stale holdings price can put unvested above the account's own balance and produce a negative asset. This mirrors `sync._refresh_investments`, which clamps the unvested remainder for the same reason ([[021-decision-plaid-vested-value-piggyback-on-sync]]). Not load-bearing enough for a replan — it narrows an edge case within the stated rule rather than changing it.
- Liability type matching lowercases `Account.type` before the set check; Plaid sends lowercase, but the column is a free-form `String(50)`.
- A2P: all three samples regenerated from the new `digest_body`, chosen so that between them they exercise every parenthetical the digest can emit — `(not counted)`, `(unvested excluded)`, `(reconnect needed)`. The campaign description gained a net-worth clause, and section 5's coupling note now names both halves (samples AND description) plus "a new parenthetical note" as a trigger.
- [reviewed — clean] completion verification: Verifier `accept`, all 9 criteria met. It reproduced rather than trusted — regenerated the three A2P samples by calling the live `digest_body` and diffed byte-for-byte against the doc (identical), hooked `before_cursor_execute` to confirm exactly one query touches `accounts` (no N+1), and re-ran the suite itself (307 passed). Judged the clamp-at-zero deviation disclosed and defensible against `sync._refresh_investments`'s own clamp.

## Review + promote 2026-08-24

- [decided] review triage (solo): four code-review findings, all FIX, all applied. F1 (medium) — the `(unvested excluded)` display flag was derived independently of the sign predicate, so a `credit` row carrying an `unvested_value` would advertise a discount `account_net_worth` never applied; fixed by extracting `_is_liability` and using it in both places, with a regression test. F2–F4 (low) — an over-long docstring line this diff introduced, a missing blank line between top-level defs, and `object` annotations on two `Optional[Decimal]` fields. Minerva audit clean on all five bearing knowledge entries; no load-bearing divergence, so no replan-vs-FIX gate fired.
- [decided] promote partition (solo): one PROMOTE (the net-worth definition → knowledge 024). MERGED into the proposal: the clamp-at-zero deviation, the `_is_liability` single-predicate rule, the `_digest_parts` seam. DISCARDed: the gate log above, as routine run mechanics. One TODO kept — the Doppler variable — filed as an issue rather than left in prose, because a forgotten env var makes the feature silently wrong rather than visibly broken.
- [decided] TODO disposition (solo): "set NET_WORTH_EXCLUDED_ACCOUNTS in Doppler" filed as honerlaw/financials#37, priority high. The `/settings` exclusion toggle stays in the proposal's Out of scope — declined at intake and better recorded as a rejected alternative than as a standing issue.

→ promoted to .minerva/knowledge/024-decision-digest-net-worth.md (2026-08-24)
